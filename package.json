{
  "name": "role-chatbot",
  "version": "0.0.1",
  "description": "Amp island RPG",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -Rf ./dist && babel ./ --out-dir ./dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./package-lock.json,./test --copy-files",
    "start": "npm run build && node dist/src/index.js"
  },
  "author": "Eduard Cot",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "express": "^4.17.1",
    "nedb-promises": "^4.0.0",
    "telegraf": "^3.32.0",
    "unique-names-generator": "^3.0.0"
  }
}
